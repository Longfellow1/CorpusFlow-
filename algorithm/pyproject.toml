[project]
name = "corpusflow-algorithm"
version = "0.1.0"
description = "CorpusFlow algorithm core service"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "httpx>=0.28.0",
  "python-dotenv>=1.1.0",
  "uvicorn>=0.34.0",
]

[tool.uv]
package = false
