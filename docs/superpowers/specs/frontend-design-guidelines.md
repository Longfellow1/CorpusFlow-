# 前端设计规范

## 交互原则

- 如无必要，勿增实体。
- 用户输入的人工控制条件，优先直接作用于当前阶段，不要跨阶段隐式生效。
- 加载反馈优先使用卡片头部图标、按钮禁用态和局部动画，不用长句提示打断阅读。
- 能用结构表达的状态，不用解释性文案补洞。

## 精调模式

- `AI扩写` 只负责阶段 1 的同类候选扩写，不直接替代阶段 2 仿写生成。
- `重新生成仿写` 负责把当前解析结果、已启用候选词和风格微调一起作用到阶段 2。
- 仿写预览区应支持快速复制，便于团队内部同步和讨论。
- 风格微调属于高优先级人工控制，数量类要求应尽量被严格遵循。
